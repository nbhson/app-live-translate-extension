{
  "name": "app-live-translate-extension",
  "version": "1.0.1",
  "description": "Live Translate (EN → VI) — Chrome Side Panel Extension",
  "type": "module",
  "scripts": {
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "build": "vite build",
    "lint": "eslint . --ext .js"
  },
  "dependencies": {
    "dompurify": "^3.1.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "dompurify": "^3.1.0",
    "jsdom": "^24.1.0",
    "vite": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
